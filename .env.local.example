# Cloud sync (optional). Copy this file to `.env.local` and fill in the values
# from your Supabase project (Dashboard → Project Settings → Data API / API Keys).
# Leave them unset to run the app fully local-first with no backend.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
